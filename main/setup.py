from setuptools import setup, find_packages

setup(
    name='mmpretrain',
    version='1',
    packages=find_packages(),
    include_package_data=True,
    description='A custom mmpretrain package for Django project',
    author='mmpretrain',
)
